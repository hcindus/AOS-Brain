
## 00:00 UTC Midnight Git Push (cron)
- Checked repo `AOS-Brain` (main): working tree clean, 0 ahead/0 behind origin/main.
- Nothing to commit or push this cycle.

## OSY-7 Orbital Shipyard Build (Captain, 2026-09-15 02:30–03:39 UTC)
Captain and I built a complete parametric orbital shipyard (OSY-7), from character detail through to a live deployed walkthrough. Full iterative session.

**Character trigger:** Captain asked for "Kael or Yael Voss" detail. There is NO "Yael Voss" — only `kael-voss` skill (original synthetic being / labor unit past lifespan). First 4 Grok renders of Kael arrived → saved as master reference set `tappylewis.cloud/assets/characters/kael-voss/` (kael-front-01..04.jpg). Drift flag: Grok rendered dark hair + striking eyes (spec said pale grey eyes); images now canonical pending Captain's final eye/hair lock. Updated `skills/kael-voss/SKILL.md` with "Locked Reference Images" section.

**New skill:** `skills/orbital-shipyard/SKILL.md` — reusable locked setting (Master Setting Anchor, design drivers, structural hierarchy, zoned layout, visual character, 5 concept types, 6 Grok prompts). Wired into kael-voss skill (environment reference).

**OpenSCAD model iterations (v1→v5):** all in `workspace/orbital-shipyard/`:
- shipyard.scad (v1 basic), shipyard_enhanced.scad (v2), shipyard_focused.scad (core bay+truss), osy7_canonical.scad, osy7_v2/v3/v4/v5.scad
- v5 (final) = `osy7_v5.scad` + `osy7_v5.stl`: double-sided assembly bay, 4 internal decks w/ floor grating + handrails + support spokes, cable trays, interior/exterior lighting, detailed habitat ring w/ modular interiors (crew quarters/lab/corridor) + artificial-gravity cues (red outward arrows + cyan spin indicators), articulated multi-joint robotic arms `robotic_arm(base_rot, arm_len)`, solar arrays w/ raised frame + winglets, framed radiators, docking ports w/ petals, service modules, antennas, nav lights.
- **Key metric:** 27 volumes, ~85k facets. All versions export "non-manifold" (20-30 separate touching volumes, not watertight) — renders/game fine, NOT 3D-print-clean. Watertight needs boolean-union pass.
- **Key params (meters):** assembly bay Ø180×250, truss 44×480, habitat ring Ø310, wall 5.5, inner clear Ø82.

**Tooling discovered:** `openscad` CLI + `xvfb-run` render PNGs (Nature colorscheme). `blender` 4.2 + `xvfb-run -b -P` for STL import/manifold check/materials/EEVEE render. No admesh/meshlab. `cairosvg` (installed via pip --break-system-packages) for SVG→PNG. No rsvg/inkscape/imagemagick for SVG.

**Blender render scripts:** `/tmp/build_render.py` (hero renders, 17-29 parts split by LOOSE, palette classify), `/tmp/build_anim.py` (turntable). Cycles 48 samples too slow (~12s/frame); EEVEE-Next `scene.eevee.taa_render_samples` fast but Mars+shipyard still ~30s/frame at 1280×720 → aborted video, pivoted to interactive.

**Final deliverable — live:** `https://tappylewis.cloud/orbital-shipyard.html`
- three.js (unpkg 0.160.0) + PointerLockControls + GLTFLoader walkthrough
- Shipyard GLB (`assets/characters/mars/osy7_shipyard.glb`, 2MB, materials baked) in low orbit above Mars GLB (`assets/characters/mars/mars.glb`, NASA, 4MB, embedded diffuse+normal, scaled 8x)
- WASD fly, mouse look, Shift boost, Space/C up/down
- **Velvet Cabaret** placed in habitat ring (stern = crew decompress zone) as glowing red "✦ THE VELVET CABARET — ENTER" world-projected marker → links to existing `velvet-cabaret-3d.html`
- Deploy = copy to `/var/www/tappylewis.cloud/` (not git)

**Mars asset (from Captain):** `/root/.openclaw/media/inbound/24881_Mars_1_6792*` → `orbital-shipyard/mars/mars.glb` (1 mesh, 2034 verts, radius ~500, embedded mars_diff.png + mars_norm.png 2048×1536) + zip with `Mars_1_6792.usdc` + `image1.jpg` (diffuse) + `image0_norm.jpg`. Extracted textures via GLB bufferView parsing (bv4=norm 507KB, bv5=diff 3.4MB).

**Grok limitation noted:** Grok cannot emit downloadable CAD binaries (.STEP/.STL/.IGES) — only source code (OpenSCAD/SVG). Real CAD files come from OpenSCAD F6 → export (which I can run locally).

**Open follow-up options (Captain may revisit):** (a) build actual cabaret interior INTO ring geometry (vs marker link), (b) tune walkthrough (spawn/speed/teleport button).

## OSY-7 Walkthrough Load Optimization (2026-09-15 03:41 UTC)
Captain flagged page loading too heavy ("Must be big, loading assets..."). Root cause: Mars GLB was 4MB (embedded 3.4MB PNG diffuse + 507KB normal). Fix: swapped Mars from GLB to textured sphere using the JPEG textures already present in the Captain's zip (`image1.jpg` diffuse 508KB, `image0_norm.jpg` normal 304KB). Deleted heavy `mars.glb` + extracted PNGs from `/var/www/tappylewis.cloud/assets/characters/mars/`. Result: total payload ~6MB → ~2.9MB (shipyard GLB 2.1MB + mars JPEGs 0.8MB). Same texture, half the download. Mars now rendered as `SphereGeometry(4000,128,96)` + `MeshStandardMaterial` with `map` + `normalMap` (normalScale 0.6).
