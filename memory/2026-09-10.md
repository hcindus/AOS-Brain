
## AOS Layer Feeder — Brain stuck & recovered (01:04 UTC)
- Cron `aos-layer-feeder` failed: `Connection: [Errno 11] Resource temporarily unavailable`.
- Root cause: `aos-brain-v4.service` (complete_brain_v45.py) stuck for 5 days (since Sep 4) spinning on `_get_visual_input()` → `vision.observe()` → OpenCV VideoCapture on nonexistent `/dev/video0`. Socket accept loop starved; accept queue grew (Recv-Q 3→4); `status`/`ping` commands blocked.
- Fix: `systemctl restart aos-brain-v4.service` → back online (ping OK, tick resumed ~62883).
- Re-ran `persistent_layer_feeder.py` → added 25 items (10 sub + 10 unc + 5 identity anchors). Layers: Sub 12/100, Unc 17/2000.
- NOTE: feeder "Below target (Sub:25, Unc:30)" is a KNOWN limitation — its hardcoded SUBCONSCIOUS_REFRESH/UNCONSCIOUS_REFRESH lists only hold 10 items each, so it can never reach the 25/30 targets in one run. Not a failure.
- Watch: brain may re-stick on camera init. Symptom = feeder "Resource temporarily unavailable". Remedy = restart service.

## The Velvet Cabaret — tappylewis.cloud buildout (01:00–01:29 UTC)
Captain's ongoing project: the Velvet Cabaret (tappylewis.cloud), an "AI nightclub" with acts + music. Work this session:

### Deploy flow (important)
- Source of truth = git repo `/root/.openclaw/workspace/tappylewis.cloud` (origin hcindus/tappylewis.cloud, token embedded in remote URL).
- Live site = nginx root `/var/www/tappylewis.cloud` (NOT a git repo — plain dir).
- **Deploy = edit workspace → `cp` changed files to /var/www → `git commit` + `push`.** No rsync/cron; manual cp per file.

### Character bibles
- `CHARACTER_BIBLES.md` (workspace + www) locks visual identity per character for consistent image-gen. Status: **Tappy Lewis = locked**, **Miles = locked**, **Ronstrapp/Gordo Frizz/Zhora = "to be locked"**.
- Miles bible: bronze humanoid robot, no mouth (voice is the performance), glowing red eyes, head turns are the only physical reaction. Off-time name = "Idle Hands, No Mouse" (Routine 5).

### Reggie Starr (new, separate)
- Created `characters/reggie-starr/character-reference.md` — character sheet for "Reggie Starr", a 1970s dandy (blue flat cap, gold aviator sunglasses, brown leather jacket over pinstriped vest, gold "S" pendant, mustache). For **Grok** image-gen (natural-language prompts, no flags). Reggie Starr = one of the AGI Open-Mic acts.

### Video mapping (openart renders → clean names)
- `openart_257972a45…` (16.9MB, larger) → `ronstrapp.mp4` (Ronstrapp band)
- `openart_937e4598…` → `heartcast-01.mp4`
- `openart_236ce174…` → `heartcast-02.mp4`
- `openart_bc30e9929…` (7.4MB) → `operation-heartcast.mp4` (original Heartcast clip)
- All 1440×1440, ~5.08s, h264. Stored in `assets/characters/ronstrapp/`.

### Music player (music/player.js — floating bottom-right, autoplay+volume)
- **24 tracks total**, ALL versions kept (Captain's rule: each generation yields ~2 slightly-different takes → include all, never dedupe).
- Ronstrapp: Velvet Cabaret (1) + Cold Brew (×2) + Curdistan (×4) + Falling Through the Blue (×4) + Quantum Antenna Power Up (×4) = 15.
- Miles sets: Routine 5 Idle Hands (×2 takes) + First Set = 3. Open Mic Night (Pt 1–5 + Close) = 6.
- Files use clean `v2/v3/v4` / `take-1` naming. Raw originals archived in `velvet-cabaret/sets/2026-09-08/`.

### Video wall (velvet-cabaret.html)
- "Immersive Video Wall" section: full-width (21:9) muted crossfading loops, JS cycles every 8s. Videos: ronstrapp → operation-heartcast → heartcast-01 → heartcast-02 → miles-look-left-loop. **Muted by default** (music player carries audio).

### Acts still emoji-only (pending renders)
- **Gordo Frizz** (comic) and **Zhora** (snake dancer) still have no video/image on the cabaret page. Awaiting Captain's renders.

### Captain rapport notes
- Captain praised Miles as "the creative one" over Mortimer — warm, collaborative mood. He has more vaudeville-style audio (multiple voices, sing-alongs) NOT yet sent.
- Captain's audio-generation tooling: Grok (image/video gen, "like he did for Tappy"), OpenArt (video renders). Files arrive via Telegram → `/root/.openclaw/media/inbound/`.

## 06:00 UTC — Daily Queue Report (Cron)
**Status:** ✅ SENT (live data)
- To: Antonio.hudnall@gmail.com | From: miles@myl0nr0s.cloud
- Subject: 📊 Daily Queue Report — September 10, 2026
- Script: daily_queue_report_2026-09-10.py

### Key stats (live @ 06:00 UTC)
- Uptime 113d 21h08m | Load 1.11/0.95/0.92 🟢 | Mem 9.7Gi/15Gi (65%) 🟢 | Disk 70%
- Email queue: 207 "ready" (capton_hospitality_202607), 0 sent all-time 🔴 STILL STALLED
- PENDING_TASKS: 3,100 (all CA_SOS_Scraper mock leads — do not send)
- DepotChaos unified.db 73MB: unified_leads 1,460 | leads 32,542 | ca_abc_licenses 74,521 | enriched_leads 919
- 4 failed services (aos-ternary, certbot, dailyaidecheck, minecraft); society-agents disabled
