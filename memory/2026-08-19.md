# 2026-08-19 — Miles Daily Log

## Daily Data Scraper + GitHub Sync — push was blocked, fixed

**Time:** ~02:00–02:17 UTC

### What happened
- Ran `scripts/daily_data_scraper.sh` (02:00) — created `data/DAILY_DATA_20260819.json`, committed fine.
- Push to GitHub FAILED: a **763 MB Java heap dump** `reggiestarr-rs80/java_pid3691757.hprof` (created 00:58, pid 3691757) had been accidentally committed (in commit `7095b28d4a` "Fix RS-80 Android build"), exceeding GitHub's 100 MB limit.

### Fix applied
1. Deleted the hprof from disk.
2. Added `*.hprof`, `*.heap`, `reggiestarr-rs80/*.hprof` to `.gitignore`.
3. Committed removal ("Remove accidental Java heap dump; gitignore *.hprof").
4. `git filter-branch --index-filter 'git rm --cached --ignore-unmatch ...'` over `origin/main..HEAD` (9 commits) to strip the blob from history.
5. Deleted `refs/original/` backup, expired reflog, ran `git gc --prune=now`.
6. Confirmed `git push origin main` → "Everything up-to-date" (repo in sync).

### Root cause
- The Android build (reggiestarr-rs80) triggered a JVM heap dump at 00:58, and the indiscriminate `git add -A` auto-commit (continuous scraper / Jordan office sync) swept it in.

### Recommendation (for Captain)
- Consider adding a size guard to the auto-commit scripts (skip/ignore files >100 MB) to prevent recurrence.
- There's a pre-existing **1.15 GB** `labs/bonsai-quant-lab/models/Bonsai-8B-Q1_0.gguf` still referenced in git history (already `.gitignore`d but in older commits). If GitHub push is ever blocked again, this is a candidate for `git filter-repo` cleanup.

### Dangling blobs
- 800 MB hprof + 156 MB unknown blob were dangling/unreferenced — pruned via `git gc --prune=now` (background, pid 3792883).

## Agent Health Check (02:19 UTC) — two issues fixed

1. **Memory 92% CRITICAL** — root cause was my own `git gc --prune=now --aggressive` (window=250) repacking the 1.15GB gguf, consuming 8.7GB RSS. Killed it; memory dropped to ~80% (12Gi/15Gi). Deferred the hprof/156MB dangling-blob cleanup (disk healthy at 73%, non-urgent).
2. **Society agents (5-Agent Civilization) down 3 days** — `society-agents.service` inactive since 08-15 16:33 (SIGTERM). Restarted; now running (marcus + civilization re-spawned).

Everything else healthy: Mortimer, Brain v4.5, BHSI v4, Mission Control, Roblox Bridge, Minecraft server, all agent factory services ACTIVE.

---

## Temporal Dark Factory — Level 5 + Solar Conquest + RS-80 (Miles session)

**Time:** ~22:47–02:19 UTC (overlaps the scraper session above)

### RiP GoR Council formalized
- **RiP GoR** = **R**oast(int) + **P**atricia → **Go** with the **R**esult. Council roster: 6 Roast personas (Contrarian 25%, Expansionist 15%, FirstPrinciples 20%, Researcher 20%, Buyer 20% → Judge) + Patricia (DMCIA specialist, was "Chief of Staff") + Captain (override).
- DarkFactory ruling: **RESHAPE** (build sandboxed Level-3+ harness, don't skip to full Level-5 on production).

### Temporal stood up on Miles VPS (this host, 31.97.6.40)
- Docker Compose `/opt/temporal/docker-compose.yml` (postgres + `temporalio/auto-setup:1.24.2` + UI `:8233`). gRPC `:7233`. Postgres port 5432 was already taken by `buzz-postgres`, so no host port mapping.
- Worker: systemd `darkfactory-worker.service` (Python venv `.venv` at `temporal/darkfactory/`, `temporalio` 1.31.0).
- **Level 5 additions**: `triage_loop.py` (scans `specs/inbox/*.json`, accepts/rejects vs mission.md, auto-submits) + `darkfactory-triage.timer` (30 min) + `deploy_blue_green` activity (blue/green symlink flip to `/var/www/darkfactory-deploy/{project}/`).
- Pipeline: validate SDK → allocate → build → verify → blind hold-out → blue-green deploy → notify (30-min durable watchdog).

### Temporal bugs fixed
- `workflow.start_timer` / `cancel_timer` DON'T exist in temporalio → replaced with `asyncio.wait_for` watchdog.
- Activity args arrive as **dicts** at the boundary (dataclasses JSON-serialized) → `execute_build` made dict-safe.
- heartbeat_timeout 60s → 15min (gradle builds are slow).

### Temporal consolidation
- Go `collections-worker` (debt collection, compiled binary) → now `collections-worker.service`, queue `collections-queue`.
- `mortimer-build/.../temporal_engine.py` → deprecated (was a mock "Temporal-like" engine, NOT real Temporal).
- `depotchaos/temporal_workflow.py` → NOT real Temporal (pre-Temporal CLI task manager); left as local daemon.

### Dark Factory queue (unfinished jobs re-run)
- Specs queued: RS-80 (apk), Cream mobile (apk), ReggieStarr Android (apk), nognog (web), neon-courier (web). Web builds COMPLETE; APK builds initially failed.
- **RS-80 Android POS** was the flagship "⏳ incomplete" — fixed 10 project-level bugs to get it building:
  - `gradle-wrapper.jar` MISSING (use global `gradle 8.5`, not `./gradlew`); missing `pluginManagement` in settings.gradle; duplicate Groovy+Kotlin DSL files (.kts removed); `android.useAndroidX=true` missing; JVM heap `-Xmx3g`; missing `backup_rules.xml`/`data_extraction_rules.xml`; missing mipmap launcher icons (copied from ReggieStarr android); JitPack repo missing; escpos-coffee import path wrong (stray `.coffee`); missing `KeyboardOptions`/`ShoppingCart` imports; `@OptIn(ExperimentalMaterial3Api)`.
  - **BUILD SUCCESSFUL** → 14.78 MB `app-debug.apk` in `/var/www/darkfactory-deploy/RS-80/`.
- Toolchain present: Java 17 (17.0.19), Gradle 8.5, `/opt/android-sdk` (sdkmanager), npx + @bubblewrap/cli 1.24.1.

### RS-80 product page (psdepot.com/RS-80.html)
- Full product page (SAM4S template), rebranded **"Reggie Starr"** (two words, NOT ReggieStarr) + "Point of Sale System" tagline. Logo at `/assets/images/reggie-starr-logo.jpg` (from Captain's image). SKU RS-80, $1,499 placeholder. Tile added to `products/index.html` Popular Products.

### Email → APK distribution
- Hostinger SMTP caps attachment size: 14.78 MB APK attachment TIMED OUT. **Use download links instead.** APK hosted at `https://psdepot.com/downloads/Reggie-Starr-RS-80.apk`.

### Solar Conquest game (fixes)
- Game = "Solar Conquest" (starship combat, HTML5 canvas), NOT the SGVD game (that was a wrong earlier guess). 3 versions: v13 (1 system), v13-2 (3 systems), v13-3 (11 systems).
- Built by "Morty on A16", hosted on **Mortimer VPS = 31.97.6.30** (NOT GitHub). `~/.ssh/mortimer_key` is CORRUPTED ("error in libcrypto"). No SSH access → couldn't deploy there.
- **Bug 1 (camera)**: chase-cam `project()` had yaw/pitch axes swapped (`tx=forward` used as horizontal, `tz=right` used as depth). Fix: pitch applies to `tx`(forward) not `tz`(right); horizontal uses `tz`(right). Ship was off-screen + no up/down look.
- **Bug 2 (autofire)**: `fireActive`/`keys` stuck true (no `touchcancel`/`blur` reset). Fix: added `touchcancel` + `pointerup` on fire button, `touchcancel` on joystick, `blur` handler.
- Deployed to **tappylewis.cloud/games/** (solar-conquest-v13.html, -v13-2.html, -v13-3.html). Committed to GitHub `AOS-Brain → solar-conquest/`.

### Key infra facts (for future)
- Host = **Miles VPS, 31.97.6.40** (Ubuntu 24.04, Docker 29.3). Mortimer VPS = 31.97.6.30 (separate, no working SSH).
- Web roots: psdepot.com = `/var/www/psdepot.com`, tappylewis.cloud = `/var/www/tappylewis.cloud`.
- nginx serves EXACT files (no .html stripping): `try_files $uri $uri/ =404`.
- GitHub remote: `hcindus/AOS-Brain` (token in remote URL).

---

## RS-80 tile + nginx gotcha (02:15–02:23 UTC)

- Added RS-80 tile to `/var/www/psdepot.com/products/index.html` → "Popular Products" section (near the BOTTOM of the page). Captain initially "didn't see it" = browser cache + it's below the fold (below Cash Drawers / Cash Registers / Other Product Categories).
- **nginx gotcha**: `/etc/nginx/sites-enabled/99-psdepot.com` has a `location /rs-80 { alias /var/www/psdepot.com/rs-80; }` block (a DIRECTORY, likely empty/nonexistent) that is DISTINCT from the `/RS-80.html` file I created. So `/RS-80.html` works but `/rs-80` (no ext) 404s via the alias. If Captain wants clean `/RS-80` URL, need to reconcile that location block (not just add a redirect).
- psdepot.com nginx: HTML is NOT cached (only static `\.(js|css|png|jpg|...)` gets `expires 1y; Cache-Control public,immutable`). So "not seeing" edits is browser cache, not server.

## Waste Emailer cron (02:22 UTC) — script DISABLED, no action
- Reminder asked to run `scripts/waste_emailer.py` + send waste report to Captain.
- Script is disabled (Captain's 2026-07-15 order "no more waste emails"): `sys.exit(0)` at top, `WASTE_RECIPIENTS = []`.
- Ran it → exit 0, no email sent. Stale cron; did NOT re-enable. No relay to Captain (he disabled it himself).
