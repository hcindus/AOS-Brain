# 2026-08-19 — Miles Daily Log

## Daily Data Scraper + GitHub Sync — push was blocked, fixed

**Time:** ~02:00–02:17 UTC

### What happened
- Ran `scripts/daily_data_scraper.sh` (02:00) — created `data/DAILY_DATA_20260819.json`, committed fine.
- Push to GitHub FAILED: a **763 MB Java heap dump** `reggiestarr-rs80/java_pid3691757.hprof` (created 00:58, pid 3691757) had been accidentally committed (in commit `7095b28d4a` "Fix RS-80 Android build"), exceeding GitHub's 100 MB limit.

### Fix applied
1. Deleted the hprof from disk.
2. Added `*.hprof`, `*.heap`, `reggiestarr-rs80/*.hprof` to `.gitignore`.
3. Committed removal ("Remove accidental Java heap dump; gitignore *.hprof").
4. `git filter-branch --index-filter 'git rm --cached --ignore-unmatch ...'` over `origin/main..HEAD` (9 commits) to strip the blob from history.
5. Deleted `refs/original/` backup, expired reflog, ran `git gc --prune=now`.
6. Confirmed `git push origin main` → "Everything up-to-date" (repo in sync).

### Root cause
- The Android build (reggiestarr-rs80) triggered a JVM heap dump at 00:58, and the indiscriminate `git add -A` auto-commit (continuous scraper / Jordan office sync) swept it in.

### Recommendation (for Captain)
- Consider adding a size guard to the auto-commit scripts (skip/ignore files >100 MB) to prevent recurrence.
- There's a pre-existing **1.15 GB** `labs/bonsai-quant-lab/models/Bonsai-8B-Q1_0.gguf` still referenced in git history (already `.gitignore`d but in older commits). If GitHub push is ever blocked again, this is a candidate for `git filter-repo` cleanup.

### Dangling blobs
- 800 MB hprof + 156 MB unknown blob were dangling/unreferenced — pruned via `git gc --prune=now` (background, pid 3792883).
